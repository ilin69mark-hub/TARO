# SEO

> Статус: `draft`.
- Индексируем: `/` (лендинг), `/spreads` (каталог), `/blog/*` (значения карт, позже), sitemap + OG-картинки раскладов.
- Закрываем (`noindex`): `/reading/*`, `/history`, `/admin/*`, API `/v1/*` (`X-Robots-Tag: noindex`).
- SSR + мета на русском, `hreflang ru` один; Schema.org `WebApplication`.
- Цель мес. 3: 30% трафика из SEO «карта дня / значение {карта}».
